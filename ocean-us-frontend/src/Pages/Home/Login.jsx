import React, { useState } from 'react'

const initialState = {
    email: "",
    password: ""
}

const Login = () => {
    const [formValue, setFormValue] = useState(initialState)
    const  { email, password } = formmValue
     
    return(
        <div>

        </div>
    )
}

export default Login